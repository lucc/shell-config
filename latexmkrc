# user configuration for latexmk
# vim: filetype=perl

$pdf_mode = 1;
$pdf_previewer = 'start open -a Preview %O %S';
$pdf_update_method = 0;
$pdflatex_silent_switch = '-interaction=batchmode -file-line-error';
$silent = 1;
